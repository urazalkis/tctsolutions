import 'package:flutter/material.dart';

class SizedBoxWidthMedium extends StatelessWidget {
  const SizedBoxWidthMedium({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 15,
    );
  }
}
