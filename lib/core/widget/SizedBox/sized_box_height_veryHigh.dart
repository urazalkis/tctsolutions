import 'package:flutter/material.dart';

class SizedBoxHeightVeryHigh extends StatelessWidget {
  const SizedBoxHeightVeryHigh({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50,
    );
  }
}
