import 'package:flutter/material.dart';

class SizedBoxHeightHigh extends StatelessWidget {
  const SizedBoxHeightHigh({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 25,
    );
  }
}
