import 'package:flutter/material.dart';

class SizedBoxWidthLow extends StatelessWidget {
  const SizedBoxWidthLow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 10,
    );
  }
}
