import 'package:flutter/material.dart';

class SizedBoxHeightMedium extends StatelessWidget {
  const SizedBoxHeightMedium({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 15,
    );
  }
}
