import 'package:flutter/material.dart';

class SizedBoxHeightLow extends StatelessWidget {
  const SizedBoxHeightLow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}
